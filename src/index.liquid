---
layout: layouts/default.liquid
---

<div class="main-sections">
  <!-- Hero Section -->
  <section class="hero">
    <h1>Привет, я Александр 👋</h1>
    <p class="lead">Технический писатель, ментор и DocOps-энтузиаст с опытом создания документации для сложных технических продуктов.</p>
    <div class="cta-buttons">
      <a href="/about" class="button primary">Обо мне</a>
      <a href="/mentorship" class="button primary">Менторство</a>
      <a href="/projects" class="button primary">Проекты</a>
    </div>
  </section>

  <!-- Latest Posts -->
  <section class="latest-posts">
    <h2>Последние публикации</h2>
    {% assign posts = collections.posts | reverse %}
    {% assign count = 3 %}
    {% include "postlist.liquid" %}
    <a href="/blog" class="articles-link">Все статьи →</a>
  </section>

  <!-- Topics -->
  {% assign tagList = collections | postTags %}
  {% if tagList.length > 0 %}
  <section class="topics">
    <h2>Темы</h2>
    <div class="tags-cloud">
      {%- for tag in tagList -%}
        <a href="/tags/{{ tag.name }}" class="tag">
          {{ tag.name }} <span class="tag-count">{{ tag.count }}</span>
        </a>
      {%- endfor -%}
    </div>
  </section>
  {% endif %}
</div>

<style>
  .main-sections {
    display: flex;
    flex-direction: column;
    gap: var(--space-7);
  }

  .hero {
    text-align: center;
    padding: var(--space-4) 0;
  }

  .lead {
    font-size: 1.25rem;
    color: var(--color-text-secondary);
    max-width: 42rem;
    margin: var(--space-4) auto;
  }

  .cta-buttons {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    margin-top: var(--space-6);
  }

  .button {
    padding: var(--space-3) var(--space-5);
    border-radius: var(--radius);
    text-decoration: none;
    font-weight: 500;
  }

  .button.primary {
    background: var(--color-primary);
    color: white;
  }

  .button.secondary {
    border: 1px solid var(--color-border);
    color: var(--color-text);
  }

  .services-grid,
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-4);
    margin-top: var(--space-4);
  }

  .service-card,
  .project-card {
    padding: var(--space-4);
    border: 1px solid var(--color-border);
    border-radius: var(--radius);
  }

  .tags-cloud {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .tag {
    display: inline-flex;
    align-items: center;
    padding: var(--space-2) var(--space-3);
    background: var(--color-background-secondary);
    border-radius: var(--radius);
    text-decoration: none;
    color: var(--color-text);
  }

  .tag-count {
    margin-left: var(--space-2);
    font-size: 0.875rem;
    color:


var(--color-text-secondary);
  }

  .learn-more {
    display: inline-block;
    margin-top: var(--space-3);
    color: var(--color-primary);
    text-decoration: none;
  }

  .articles-link {
    display: inline-block;
    margin-top: var(--space-4);
    color: var(--color-primary);
    text-decoration: none;
  }

  @media (max-width: 768px) {
    .cta-buttons {
      flex-direction: column;
      align-items: stretch;
    }

    .button {
      text-align: center;
    }
  }
</style>
